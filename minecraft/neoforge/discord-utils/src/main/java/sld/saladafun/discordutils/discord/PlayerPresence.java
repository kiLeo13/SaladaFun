package sld.saladafun.discordutils.discord;

/** Describes whether a Minecraft player joined or left the server. */
public enum PlayerPresence {
    /** The player completed login. */
    JOINED,
    /** The player disconnected. */
    LEFT
}
